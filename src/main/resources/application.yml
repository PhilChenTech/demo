spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:member # h2 database ????
    driver-class-name: org.h2.Driver # ??driver
    username: sa # database ???
    password:  # database ??

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect